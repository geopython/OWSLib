Imports and initialize

    >>> from __future__ import (absolute_import, division, print_function)
    >>> from owslib.wfs import WebFeatureService

    >>> wfs = WebFeatureService(url='http://geoserv.weichand.de:8080/geoserver/wfs', version='2.0.0', timeout=30)

Test StoredQueries

    >>> print(wfs.storedqueries[1].id)
    'http://inspire.ec.europa.eu/operation/download/GetSpatialDataSet'

    >>> print(wfs.storedqueries[1].title)
    'GetDataSetByIdLanguageAndCrs (TG 3.1)'

    >>> print(wfs.storedqueries[1].parameters[0].name)
    'DataSetIdCode'